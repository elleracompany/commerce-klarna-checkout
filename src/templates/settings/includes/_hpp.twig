<h2>Hosted Payment Page Settings</h2>
{% import "_includes/forms" as forms %}

{{ forms.elementSelectField({
    label: gateway.getAttributeLabel('logo_id'),
    id: 'logo_id',
    name: 'logo_id',
    elements: [gateway.getLogoAsset()],
    elementType: 'craft\\elements\\Asset',
    selectionLabel: "Choose"|t('app'),
    limit: 1,
    errors: gateway.getFirstError('logo_id')
}) }}

{{ forms.elementSelectField({
    label: gateway.getAttributeLabel('background_id'),
    id: 'background_id',
    name: 'background_id',
    elements: [gateway.getBackgroundAsset()],
    elementType: 'craft\\elements\\Asset',
    selectionLabel: "Choose"|t('app'),
    limit: 1,
    errors: gateway.getFirstError('background_id')
}) }}

{{ forms.textField({
    label: gateway.getAttributeLabel('back'),
    name: 'back',
    value: gateway.back,
    instructions: 'Back page. If the users clicks on "go back" from the klarna checkout page. Passes the transaction number as parameter \'transaction_token\'' | t('klarna-checkout'),
    errors: gateway.getFirstError('success')
}) }}

{# forms.multiSelectField({
    label: 'Available Payment Methods'|t('commerce'),
    instructions: "Select the payment methods you want to be available. Leave blank for all payment methods."|t('klarna-commerce'),
    id: 'methods',
    name: 'methods',
    options: gateway.getAvailablePaymentMethods(),
    values: gateway.methods,
    errors: gateway.getErrors('methods'),
    class: 'selectize fullwidth',
}) #}